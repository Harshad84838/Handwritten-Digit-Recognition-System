# Handwritten-Digit-Recognition-System
A Deep Learning model built with Keras and TensorFlow to recognize handwritten digits (0-9). Features a Sequential Neural Network trained on the MNIST dataset with a custom image testing script using OpenCV.
